<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wikipedia Loader</title>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-4">Wikipedia Article Viewer</h1>

        <!-- Search Form -->
        <form method="get" action="/load-wiki" class="mb-4">
            <div class="input-group">
                <input type="text" name="article" class="form-control" placeholder="Search Wikipedia article..." />
                <button class="btn btn-primary" type="submit">Search</button>
            </div>
        </form>

        <!-- Article Container -->
        <div class="card">
            <% if (articleData) { %>
                <div class="card-body">
                    <h2>
                        <%= articleData.title %>
                    </h2>
                    <p>
                        <%= articleData.extract %>
                    </p>
                    <a href="<%= articleData.content_urls.desktop.page %>" target="_blank"
                        class="btn btn-secondary">Read more on Wikipedia</a>
                </div>
                <% } else { %>
                    <div class="card-body">
                        <p class="text-danger">Error loading article. Please try again.</p>
                    </div>
                    <% } %>
        </div>
    </div>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>